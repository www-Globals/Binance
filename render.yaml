services:
  - type: web
    name: arb-bot
    env: python
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: python arb_bot.py
    plan: free
    envGroup: Binance
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
