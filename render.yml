services:
  - type: web
    name: payload-host
    runtime: python
    plan: free
    buildCommand: pip install flask
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
